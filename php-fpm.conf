; Numanski PHP-FPM pool override — laadt na www.conf (zz-prefix).
; Standaard verwijdert PHP-FPM omgevings-variabelen uit het worker-proces;
; wij hebben SMTP_* nodig in getenv(), dus zetten clear_env uit.
[www]
clear_env = no
